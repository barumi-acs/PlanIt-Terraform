apiVersion: v1
kind: Service
metadata:
  name: planit-user-svc
  namespace: planit-dev
  labels:
    app: user

spec:
  type: ClusterIP

  selector:
    app: user

  ports:
    - name: http
      port: 8081
      targetPort: 8081

    - name: grpc
      port: 9091
      targetPort: 9091