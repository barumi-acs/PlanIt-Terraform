apiVersion: v1
kind: Service

metadata:
  name: planit-schedule-svc
  namespace: planit-dev

spec:
  type: ClusterIP

  selector:
    app: schedule

  ports:
    - name: http
      port: 8082
      targetPort: 8082

    - name: grpc
      port: 9092
      targetPort: 9092