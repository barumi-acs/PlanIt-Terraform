apiVersion: v1
kind: Service

metadata:
  name: planit-insight-svc
  namespace: planit-dev

spec:
  type: ClusterIP

  selector:
    app: insight

  ports:
    - name: http
      port: 8084
      targetPort: 8084

    - name: grpc
      port: 9094
      targetPort: 9094